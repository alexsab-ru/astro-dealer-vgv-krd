---
import PageLayout from '@/layouts/PageLayout.astro';
import modelsData from '@/data/models.json';
import { Icon } from 'astro-icon/components';

const models = modelsData.models.filter((model) => model.show && model.status === 'enable');

const modelsWithManuals = models.length > 0 
    ? models.map(model => ({
        ...model,
        url: `https://cdn.alexsab.ru/manuals/vgv/${model.id}.pdf`
    }))
    : [];
---

<PageLayout
    h1="Руководства"
    title="Руководства"
    description="Ознакомьтесь с Руководством к автомобилям VGV"
    breadcrumb={[{name:"Руководства",href:""}]}
>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 auto-rows-fr">
        {modelsWithManuals.map((model) => (
            <div class="shadow-md rounded-lg overflow-hidden flex flex-col h-full">
                <img src={model.thumb} alt={model.name} class="w-full h-auto object-cover" />
                <div class="p-4 mt-auto">
                    <a href={model.url} target="_blank" rel="noopener noreferrer" class="flex items-center gap-2 text-blue-600 hover:text-blue-800 transition-colors">
                        <Icon name="mdi:download" class="w-6 h-6" />
                        <span>Руководство для VGV {model.name}</span>
                    </a>
                </div>
            </div>
        ))}
    </div>

</PageLayout>